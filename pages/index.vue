<template>
  <section class="prose ">

    <ContentDoc path="me" class="slide-enter-content" />

    <!-- <ContentRenderer :value="result"></ContentRenderer> -->
  </section>
</template>

<script setup>
  
  // const {data} = await $fetch("/api/hello")
  // const contentQuery = queryContent('me').findOne()

  // console.log(data)

  // const result = ref(null); // 使用ref创建一个响应式引用

  // onMounted(
  //   async () => {
  //   const contentQuery = queryContent('me').findOne();
  //   result.value = await contentQuery; // 使用await获取结果并将其赋值给result.value
  // }
  
  // );

</script>
